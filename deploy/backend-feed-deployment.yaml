apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-feed
  labels:
    app: udagram-api-feed
spec:
  replicas: 2
  selector:
    matchLabels:
      app: udagram-api-feed
  template:
    metadata:
      labels:
        app: udagram-api-feed
    spec:
      containers:
      - name: backend-feed
        image: gauravsinghaec/udagram-api-feed:v1
        envFrom:
          - secretRef:
              name: udagram-secret
          - configMapRef:
              name: udagram-config-map
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 100Mi
          limits:
            memory: 200Mi